name: flutter_application_1
description: A new Flutter project.

# 🔑 Add the environment constraint here
environment:
  sdk: '>=3.1.0 <4.0.0' # Use a standard range for Dart 3

dependencies:
  flutter:
    sdk: flutter
    # Add or ensure this line exists:
  shared_preferences: ^2.2.2
  # Add necessary packages for icons/styling (minimal for now)
  font_awesome_flutter: ^10.7.0
  http: ^1.2.0
  cupertino_icons: ^1.0.6
  # Optional: For better state management later (using simple setState for now)
  # provider: ^6.1.2